<section id="competences" class="relative w-full min-h-screen overflow-hidden bg-black text-white px-4 md:px-12">
  <!-- Effet de fond dynamique -->
  <div class="absolute inset-0 opacity-30 pointer-events-none bg-grid-animation"></div>

  <div class="container mx-auto max-w-5xl text-center relative z-10">
    <!-- Phrase d'accroche -->
    <div class="h-screen flex flex-col justify-center items-center fade-in">
      <h2 class="text-5xl md:text-7xl font-extrabold leading-snug text-gradient">
        <span>Le Digital n'attend pas.</span><br>
        <span>Votre marque non plus.</span>
      </h2>
    </div>

    <!-- Questionnement impactant -->
    <div class="h-screen flex flex-col justify-center items-center fade-in">
      <p class="text-2xl md:text-4xl font-light max-w-3xl text-center px-4">
        Avez-vous déjà ressenti que votre site ne reflète pas la vraie valeur de votre marque ?
      </p>
    </div>

    <!-- Bloc du problème -->
    <div class="h-screen flex flex-col justify-center items-center fade-in">
      <p class="text-lg md:text-2xl max-w-2xl text-gray-300 text-center px-6 md:px-0">
        Vous avez un produit ou un service incroyable, mais votre présence digitale n'est pas à la hauteur.<br>
        Votre site est lent, obsolète ou invisible ? Votre branding manque d'impact ?
      </p>
    </div>

    <!-- Compétences -->
    <div class="space-y-32">
      <div class="h-screen flex flex-col justify-center items-center fade-in">
        <img src="/img/sol.webp" alt="Développement Web3" class="w-40 md:w-56 mx-auto mb-6 moving-image">
        <h3 class="text-3xl md:text-5xl font-bold mb-4">Développement Web3</h3>
        <p class="text-lg md:text-2xl max-w-3xl text-center px-4">
          Intégrez la blockchain à votre projet pour une sécurité absolue, des transactions instantanées et une expérience utilisateur futuriste.
        </p>
      </div>

      <div class="h-screen flex flex-col justify-center items-center fade-in">
        <img src="/img/web.webp" alt="Création de Sites Web" class="w-40 md:w-56 mx-auto mb-6 moving-image">
        <h3 class="text-3xl md:text-5xl font-bold mb-4">Création de Sites Web</h3>
        <p class="text-lg md:text-2xl max-w-3xl text-center px-4">
          Des sites web modernes, rapides et immersifs qui transforment chaque visiteur en client.
        </p>
      </div>

      <div class="h-screen flex flex-col justify-center items-center fade-in">
        <img src="/img/business.webp" alt="Stratégie Digitale" class="w-40 md:w-56 mx-auto mb-6 moving-image">
        <h3 class="text-3xl md:text-5xl font-bold mb-4">Stratégie & Consulting</h3>
        <p class="text-lg md:text-2xl max-w-3xl text-center px-4">
          Une vision claire, un plan d’action précis et des résultats mesurables pour propulser votre projet.
        </p>
      </div>

      <div class="h-screen flex flex-col justify-center items-center fade-in">
        <img src="/img/drone.webp" alt="Création de Contenu" class="w-56 md:w-72 mx-auto mb-6 moving-image">
        <h3 class="text-3xl md:text-5xl font-bold mb-4">Création de Contenu</h3>
        <p class="text-lg md:text-2xl max-w-3xl text-center px-4">
          Du contenu vidéo & photo aérien qui apporte une nouvelle dimension à votre marque.
        </p>
      </div>
    </div>

    <!-- Preuve sociale & Call-to-action -->
    <div class="h-screen flex flex-col justify-center items-center fade-in">
      <p class="text-xl md:text-3xl font-light max-w-3xl text-center text-gray-300 px-4">
        Des centaines de clients font confiance à InnovPower pour leur transformation digitale.<br>
        Et vous ?
      </p>
      <a href="/contact" class="mt-8 bg-white text-black px-8 py-4 rounded-lg text-lg font-semibold shadow-lg hover:bg-gray-300 transition">
        Démarrons votre projet
      </a>
    </div>
  </div>

  <!-- CTA Fixe aligné à gauche -->
  <div class="fixed bottom-6 left-6 p-3 z-50">
    <a href="/contact" class="bg-gradient-to-br from-blue-500 to-purple-500 text-white px-6 py-3 rounded-full shadow-lg hover:scale-105 transition">
      Travaillons Ensemble 🚀
    </a>
  </div>
</section>

<style>
  /* Fond tech */
  .bg-grid-animation {
    background-image: radial-gradient(circle, rgba(255, 255, 255, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: moveGrid 20s linear infinite;
  }

  @keyframes moveGrid {
    0% { background-position: 0 0; }
    100% { background-position: 50px 50px; }
  }

  /* Texte avec gradient futuriste */
  .text-gradient {
    background: linear-gradient(to right, #ffffff, #D4AF37);
    -webkit-background-clip: text;
    color: transparent;
  }

  /* Animation d’apparition */
  .fade-in {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.8s ease, transform 0.8s ease;
  }

  /* Images dynamiques */
  .moving-image {
    transition: transform 0.3s ease-out;
  }

  .moving-image:hover {
    transform: scale(1.1);
  }
</style>

<script>
  // Observer pour déclencher les animations au scroll
  const observer = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        (entry.target as HTMLElement).style.opacity = "1";
        (entry.target as HTMLElement).style.transform = "translateY(0)";
      }
    });
  }, { threshold: 0.3 });

  document.querySelectorAll(".fade-in").forEach(element => observer.observe(element));
</script>
